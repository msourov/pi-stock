import { Loader } from "@mantine/core";

const AppLoader = () => {
  return (
    <div className="flex justify-centeritems-center py-10">
      <Loader />
    </div>
  );
};

export default AppLoader;
